# 🗺️ Coder LLM Finetune — 개발 계획

## 프로젝트 목표

GPT-4o-mini(유료 API)로 동작하는 ai-coding-test-assistant를 도메인 특화 파인튜닝 모델로 교체.
"작은 모델도 좁은 도메인에서는 대형 API를 대체할 수 있는가"를 데이터로 검증한다.

---

## 현재 완료된 것

- [x] 프로젝트 구조 설계 / PLAN.md 작성
- [x] 프로그래머스 Level 1~2 문제 URL 수집 (274개, API 방식)
- [x] 문제 본문 파싱 + SQL 문제 필터링 (228개 코딩 문제 추출)
- [x] GPT-4o-mini 학습 데이터 생성 (힌트 / 접근법 / 정답, 681개) — v1
- [x] v1 QLoRA 파인튜닝 (3 epochs, loss 1.077→0.47, acc 87.8%)
- [x] v1 비교 평가 → **정답 코드 생성 실패 확인**
- [x] 실패 원인 분석 (데이터 오염 / 양 부족 / 에포크)
- [x] GitHub 공개 풀이 수집 (132개 레포, 190/228 커버, 2,327개)
- [x] v2 데이터셋 재구성 (GPT 힌트·접근법 + GitHub 정답, 2,783개)
- [x] jsonl 변환 (train 2,504 / val 279)
- [x] v2 QLoRA 파인튜닝 완료 (3 epochs, loss 1.009→0.396, acc 92.0%)
- [x] v2 비교 평가 → **solution 태스크 여전히 실패**
- [x] 추가 원인 분석 → **정규식 버그로 학습 데이터 코드가 중간에 잘림 확인**
- [x] `extract_solution_func` 수정 (빈 줄 포함 함수 전체 추출)
- [ ] GitHub 풀이 재수집 (진행 중)
- [ ] 데이터셋 v3 재구성 및 학습
- [ ] v1 / v2 / v3 비교 평가

---

## Phase 1 — 데이터 수집 및 전처리

- [x] 프로그래머스 문제 크롤링 스크립트 작성 (Playwright + API)
- [x] GPT-4o-mini로 힌트 / 접근법 / 정답 코드 생성 (문제당 3쌍)
- [x] GitHub REST API로 실제 통과 정답 코드 수집 (2,327개)
- [x] 데이터셋 v2 구성: 힌트·접근법(GPT) + 정답(GitHub) 결합
- [x] Hugging Face 학습 포맷(jsonl)으로 변환
- [x] 데이터셋 규모: 2,783개 (목표 2,000개 초과 달성)

**초기 가설:** GPT-4o-mini가 생성한 데이터 품질이 그대로 파인튜닝에 충분할 것이다.
**결과:** 힌트/접근법은 충분했으나 **정답 코드는 GPT 생성물이 오염되어 부적합**. GitHub 실제 통과 코드로 교체해 해결.

---

## Phase 2 — 파인튜닝 환경 세팅

- [x] Qwen2.5-Coder-7B-Instruct 허깅페이스 다운로드
- [x] CUDA 11.8 + transformers + peft + trl 환경 구성
- [x] QLoRA 설정값 결정 (r=16, alpha=32, dropout=0.05)
- [x] 학습 스크립트 작성 (`train.py`)
- [x] Windows 환경 이슈 해결 (cp949 인코딩, BFloat16 미지원)

---

## Phase 3 — v1 학습 및 한계 발견

- [x] v1 학습 (GPT 데이터 681개, 3 epochs)
- [x] 학습 loss 곡선 기록 (train 1.077→0.47, val 0.619→0.55)
- [x] 동일 문제 10개로 GPT-4o-mini vs v1 비교
- [x] 평가 결과 정리 (`evaluation/results_epoch3.json`)

**가설:** GPT 데이터 681개로 Level 2 이하에서 GPT 대비 80% 품질이 나올 것이다.
**결과:** 힌트는 60~70% 수준으로 사용 가능. 정답 코드는 실패(로직 오류·파라미터 불일치·엉뚱한 풀이). 가설 기각.
**교훈:** train/val loss가 좋아도 실제 태스크 품질을 보장하지 않는다.

---

## Phase 4 — 데이터 재구성 (v2)

- [x] GitHub 풀이 수집 스크립트 작성 (`collect_github.py`)
- [x] 제목 정규화 매칭 + `def solution` 추출 + 중복 제거
- [x] 중간 저장 / 재개 로직 (대량 수집 안정화)
- [x] v2 데이터셋 빌드 스크립트 작성 (`build_dataset.py`)
- [x] 정답 출처를 GPT 생성물 → GitHub 통과 코드로 교체

---

## Phase 5 — 비교 실험 (한 번에 한 변수)

| 실험 | 정답 데이터 | 샘플 | epochs | 상태 |
|---|---|---|---|---|
| v1 | GPT 생성 | 681 | 3 | 완료 (실패) |
| v2 | GitHub (코드 잘림) | 2,783 | 3 | 완료 (실패) |
| v3 | GitHub (수정 후) | 재수집 중 | 3 or 5 | 대기 |

- [x] v2 학습 완료 (train loss 1.009→0.396, val loss 0.491→0.338, acc 92.0%)
- [x] v2 평가 → solution 태스크 실패 지속
- [x] 원인 분석 → 정규식 버그로 학습 데이터 코드 잘림 확인
- [x] `extract_solution_func` 수정
- [ ] GitHub 풀이 재수집
- [ ] 데이터셋 재구성 및 jsonl 변환
- [ ] v3 학습
- [ ] v3 평가
- [ ] v1/v2/v3 정량 비교표 작성

**가설 A (v1→v2):** 정답 데이터를 실제 통과 코드로 바꾸고 양을 4배로 늘리면 코드 생성 품질이 크게 개선된다.
**결과:** 기각. 데이터 수집 버그(정규식)로 인해 코드가 잘린 상태로 학습됨. 재수집 후 재검증 필요.

**가설 B (v2→v3):** 동일 데이터에서 에포크를 3→5로 늘리면 추가 개선이 있다 (단, 과적합 주의).
**결과:** 미검증 (재수집 후 진행 예정)

---

## Phase 6 — ai-coding-test-assistant 연결

- [ ] FastAPI에 로컬 모델 서빙 엔드포인트 추가 (`local_client.py`)
- [ ] `LLM_PROVIDER=local` 환경변수로 전환 가능하도록 구현
- [ ] GPT-4o-mini vs 로컬 모델 응답속도 비교 (ms)
- [ ] 비용 절감 효과 계산 (API 호출 횟수 기준)

---

## Phase 7 — 정리 및 문서화

- [ ] 실험 결과 표 정리 (v1/v2/v3 품질 비교)
- [ ] 한계 및 실패 케이스 정직하게 기록
- [x] README 최신화
- [ ] 정답 미수집 38개 문제 보강 여부 결정
- [ ] 모델 가중치 허깅페이스 Hub 업로드 (선택)

---

## 참고

- 각 Phase 완료 후 정량 측정 결과 README에 인라인 기록
- 가설이 틀렸거나 기대 이하 결과가 나와도 그대로 기록 (결론만 쓰지 말 것)
- "초기 가설 → 한계 → 대안 → 결과" 구조 유지
- 비교 실험은 한 번에 하나의 변수만 변경