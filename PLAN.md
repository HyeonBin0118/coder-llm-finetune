# Coder LLM Finetune — 개발 계획 및 회고

이 문서는 실험 설계 의도, 각 버전의 변경 이유, 실패 분석, 회고를 기록한다.
정량 결과와 사용법은 [README.md](./README.md) 참조.

## 프로젝트 목표

GPT-4o-mini(유료 API)로 동작하는 ai-coding-test-assistant를 도메인 특화 파인튜닝 모델로 교체할 수 있는지 검증한다.

핵심 질문: "작은 모델(7B)도 좁은 도메인에서 대형 API를 대체할 수 있는가?"

---

## 진행 상황

| Phase | 내용 | 상태 |
|---|---|---|
| 1 | 데이터 수집 및 전처리 | 완료 |
| 2 | 파인튜닝 환경 구성 | 완료 |
| 3 | 비교 실험 (v1~v4) | 완료 |
| 4 | 추가 개선 시도 및 한계 확인 | 완료 |
| 5 | GPT-4o-mini vs v4 비교 평가 | 완료 |
| 6 | Evol-Instruct 데이터 확장 | 완료 |
| 7 | IFD + K-Means 선별 및 v5 학습 | 완료 |
| 8 | DPO 데이터 생성 및 학습 (v1) | 완료 |
| 9 | DPO 데이터 재구성 및 재학습 (v2) | 완료 |
| 10 | LoRA rank 증대 시도 (v6) - 기각 | 완료 |
| 11 | 학습 데이터 검증 및 정제 시도 (v7) - 기각 | 완료 |
| 12 | 학습 데이터 규모 확장 (v8) | 완료 |
| 13 | v5 재현 실험 및 30문제 확장 평가 | 완료 |
| 14 | 경량 데이터 정제(중복 제거) 및 v9 학습, 최종 종합 평가 | 완료 |

---

## Phase 1 — 데이터 수집

### 문제 URL 수집
초기 시도는 Playwright로 프로그래머스 페이지의 DOM을 직접 파싱하는 방식이었으나, Level 필터가 JavaScript로 동작해 headless 환경에서 적용되지 않았다. 브라우저 네트워크 탭을 분석해 내부 API(/api/v2/school/challenges/)를 발견하고, Playwright로 세션을 유지한 채 API를 직접 호출하는 방식으로 전환했다.
- 수집 결과: 274개

### 문제 본문 파싱
각 문제 URL을 Playwright로 접근해 설명/제한사항/입출력 예시를 추출. SQL 문제는 파인튜닝에 부적합해 필터링.
- 파싱 결과: 228개 (SQL 46개 제외)

### 학습 데이터 생성 (v1용)
GPT-4o-mini로 힌트/접근법/정답 코드를 생성. GPT의 응답을 "교사"로 삼는 Knowledge Distillation 구조.
- 227개 문제 x 3가지 = 681개 샘플

---

## Phase 2 — 파인튜닝 환경

모든 실험에서 학습 설정은 동일하게 유지하고, 데이터와 프롬프트만 변경해 변수의 영향을 분리 측정했다.

| 항목 | 값 |
|---|---|
| GPU | RTX 3060 (12GB) |
| 베이스 모델 | Qwen2.5-Coder-7B-Instruct |
| 양자화 | 4bit NF4 |
| LoRA r / alpha / dropout | 16 / 32 / 0.05 |
| 학습 가능 파라미터 | 40,370,176 (0.53%) |
| epochs | 3 |
| learning rate | 2e-4 (cosine) |

---

## Phase 3 — 비교 실험 (v1 ~ v4)

### v1 — 베이스라인
GPT-4o-mini 생성 데이터를 그대로 학습.
- 힌트/접근법: GPT 대비 60~70% 수준 사용 가능
- 정답 코드: 완전 실패 (로직 오류, 파라미터 불일치, return -1 회피 다수)

원인: (1) GPT 생성 정답 코드 자체가 틀린 경우가 있어 오염된 교사 신호로 학습, (2) solution 샘플 227개로 절대 부족.

교훈: train/val loss가 좋아도 실제 태스크 품질을 보장하지 않는다.

### v2 — GitHub 통과 코드로 교체
정답 코드만 GitHub 실제 통과 코드로 교체 (681 -> 2,783, 4배).
- val loss 39% 감소했으나 실제 코드 품질은 v1과 차이 없음

원인: extract_solution_func 정규식이 빈 줄을 만나면 함수 추출을 중단하는 버그. 학습 데이터 상당수가 잘린 코드였음.

교훈: 데이터 양만 늘려서는 부족하다. 품질이 더 중요하다.

### v3 — 정규식 버그 수정
함수 추출 로직을 들여쓰기 기반으로 교체 후 재수집 (2,340개).
- val loss 추가 22% 감소했으나 파라미터 오류 지속

원인: 모델이 일관되게 잘못된 파라미터명 사용 (def solution(signals) -> def solution(n, k)). 학습 프롬프트에 함수 시그니처 정보가 없어 문제 설명만으로 추론해야 했음.

교훈: 모델이 학습할 정보가 프롬프트에 충분히 포함됐는지 점검해야 한다.

### v4 — 함수 시그니처 프롬프트 추가
solution 프롬프트에 함수 시그니처 한 줄 추가.
- val loss 추가 23% 감소, 파라미터 오류 완전 해결

| 문제 | v3 | v4 |
|---|---|---|
| 가장 많이 받은 선물 | 실패 | 성공 (friends, gifts) |
| 붕대 감기 | 실패 | 성공 (bandage, health, attacks) |
| 이웃한 칸 | 실패 | 성공 (board, h, w) |
| 데이터 분석 | 실패 | 성공 (data, ext, val_ext, sort_by) |

교훈: 프롬프트 설계 한 줄이 학습 효율에 결정적으로 기여한다.

### v4의 잔존 한계
파라미터는 정확하나 함수 내부 로직이 틀린 케이스가 남음:
- 반환값 타입 불일치 (숫자 대신 이름 반환)
- 시간 누적 처리 로직 누락
- 미정의 변수(dh, dw, color, level) 사용
- 로직 완전 오류

추정 원인: (1) GitHub 수집 코드 자체의 품질 한계(실제 통과 미검증), (2) 7B 모델 일반화 한계.

---

## Phase 4 — 추가 개선 시도 및 한계 확인

### 시도 1 — 학습 데이터 품질 필터링
5줄 미만 / return 없음 / return -1만 / 미정의 변수 사용 코드 제거.
- 2,340개 -> 2,030개 (13% 필터링)

### 시도 2 — description 전체 사용
800자 자르기 -> 전체(평균 1,500~3,000자) 사용 시도.

### 중단 이유 — RTX 3060 12GB VRAM 한계
| 설정 | 결과 |
|---|---|
| description 전체 + batch=2 | OOM (42GB 필요) |
| description 1500자 + batch=2 | OOM |
| description 800자 + batch=1, grad_accum=8 | 학습 가능하나 약 100시간 소요 |

판단: 무작정 데이터를 늘리는 대신 논문 기반 데이터 선별 전략(Phase 6~7)으로 방향 전환.

---

## Phase 5 — GPT-4o-mini vs v4 비교 평가

동일 문제 10개로 정량 비교. (결과 수치는 README 참조)

결론: 힌트 태스크는 응답 속도/비용 측면에서 실용 가능. 코드 생성은 7B 모델 한계로 완전 대체 실패. GPT 완전 대체에는 더 큰 모델(13B+) 또는 코드 실행 기반 데이터 검증 파이프라인 필요.

---

## Phase 6 — Evol-Instruct 데이터 확장

### 근거 논문
Data-efficient LLM Fine-tuning for Code Generation (arXiv:2504.12687, 2025)

핵심 인사이트: 대량 합성 데이터엔 저품질 샘플이 40~60% 섞여있어, 전체를 학습시키기보다 고품질 40%만 선별하는 게 더 효율적. (풀 데이터 66.1% -> 40% 선별 66.9%)

### Evol-Instruct 방식
기존 227개 문제를 GPT-4o-mini로 3가지 변형:
- 제약 추가 ("반복문 없이 풀어라")
- 규모 확장 (입력 크기 100배)
- 재귀 변환 ("재귀 함수만 사용")

결과: 681개 + 변형 1,362개 = 총 3,848개 (train 3,463 / val 385)

---

## Phase 7 — IFD + K-Means 선별 및 v5 학습

### 논문 3단계 방법론
1. K-Means 클러스터링(k=10): sentence-transformer 임베딩으로 10개 군집 분류, 유형별 분포 유지
2. IFD 점수 계산: IFD(C|I) = PPL(C|I) / PPL(C), 높을수록 고난도/고가치 샘플
3. 상위 40% 선별: 각 클러스터에서 IFD 높은 순 40% 선별

선별 결과: 3,463개 -> 1,381개 (39.9%) / approach 195, hint 213, solution 973

### 환경 적응
논문은 A100 4장(320GB) 기준. RTX 3060 12GB에서는 IFD 계산 시 4bit 양자화 적용. 양자화는 perplexity 비율 계산에 영향 없어 방법론 핵심은 동일 유지. IFD 계산 약 41분 소요.

### v5 학습 설정 변경
| 항목 | v4 | v5 |
|---|---|---|
| 학습 데이터 | 2,796 | 1,381 (IFD 선별) |
| max seq length | 2048 | 512 |
| batch size | 2 | 1 |
| gradient accumulation | 4 | 8 |
| gradient checkpointing | 비활성 | 활성 |

### v5 학습 결과
- train loss: 1.23 -> 0.13 (89% 감소)
- val loss: 0.451 -> 0.222 (51% 감소)
- 학습 시간: 34시간 18분 (RTX 3060 기준)

### v4 vs v5 비교 평가 결과

스크립트: compare_v4_v5.py / 결과: evaluation/compare_v4_v5.json

| 항목 | v4 | v5 |
|---|---|---|
| 파라미터 정확도 | 8/10 | 10/10 |
| 코드 완전 정답 | 0/10 | 3/10 |
| hint 응답 시간 | 8,153ms | 177,516ms |
| solution 응답 시간 | 13,767ms | 119,721ms |

정답 문제 (v5): 이웃한 칸, 달리기 경주, 퍼즐 게임 챌린지

응답 시간 증가 원인 분석:
v5는 MAX_LENGTH=512로 학습했는데, 학습 시퀀스가 짧아지면서 EOS 토큰 생성 타이밍을 제대로 학습하지 못한 것으로 추정. max_new_tokens=512까지 꽉 채워 생성하는 경향이 생김. 실서비스 적용 시 max_new_tokens를 256 이하로 제한하면 해결 가능.

핵심 가설 E 검증 결과: 부분 확인
- 파라미터 정확도: v4 8/10 -> v5 10/10 (완전 해결)
- 코드 완전 정답: v4 0/10 -> v5 3/10 (유의미한 개선)
- IFD 기반 고품질 데이터 선별이 코드 생성 품질 향상에 효과적임을 확인

### v4 vs v5 token accuracy 재평가

기존 README/PLAN.md에 기록된 v1~v4 token accuracy(87.8%~95.1%)는 계산 코드가 프로젝트에 없어 재현 불가능한 값으로 판명(train.py, 다른 .py 파일들에 accuracy/compute_metrics 로직 없음 확인됨). 이에 새 스크립트 compute_token_accuracy.py를 작성해 v4, v5를 동일 data/val.jsonl(385개) 기준으로 재측정:

- v4: 91.7%, v5: 94.1% (재현 가능한 정확한 수치)
- 측정 방식: 다음 토큰 예측 정확도(logits.argmax vs shift된 input_ids 비교), 4bit 양자화 모델로 forward pass만 수행

교훈: 측정 코드를 별도로 보관하지 않으면 같은 지표를 재현할 수 없다. 모든 평가 스크립트는 결과 수치와 함께 반드시 버전 관리해야 한다.

---

## Phase 8 — DPO 데이터 생성 및 학습 (v1)

### 배경
SFT는 "정답이 무엇인가"는 학습하지만 "정답 중 어느 쪽이 더 좋은가"는 구분 못한다. DPO(Direct Preference Optimization)가 이 간극을 채운다. 같은 정답이어도 더 Pythonic하고 효율적인 코드를 선호하도록 모델을 학습시킨다.

### 데이터 생성
GPT-4o-mini로 227개 chosen/rejected 쌍 생성:
- chosen: 깔끔하고 Pythonic한 풀이 (시간복잡도 최적화)
- rejected: 동작하지만 비효율적인 풀이 (반복문 중첩, 불필요한 변수)

### DPO v1 학습 결과

| 항목 | 값 |
|---|---|
| 학습 데이터 | chosen/rejected 204쌍 (train) |
| 학습 시간 | 54분 42초 |
| train loss | 0.683 -> 0.587 (14% 감소) |
| rewards/accuracies | 0.70 -> 0.925 (92.5%) |
| rewards/margins | 0.020 -> 0.242 (12배 증가) |

rewards/accuracies 92.5%: 모델이 10쌍 중 9쌍에서 chosen(좋은 코드)에 더 높은 확률을 부여함. DPO 학습이 효과적으로 선호도를 학습했음을 확인.

### 트러블슈팅 — trl 버전 이슈
trl 0.8.6, 0.9.6에서 4bit 양자화 모델과의 호환성 문제(NoneType 오류, 텐서 크기 불일치)가 지속 발생. trl 0.11.4로 업그레이드 후 해결. DPO 전용 가상환경(dpo_env) 분리 운영.

### DPO v1 vs v5 코드 품질 비교 평가

compare_v4_v5.py를 확장한 compare_v4_v5_dpo.py로 v4, v5, DPO 3개 모델을 동일 10문제로 비교.

1차 평가 (max_new_tokens=256):

| 항목 | v4 | v5 | DPO v1 |
|---|---|---|---|
| 파라미터 정확도 | 8/10 | 10/10 | 10/10 |
| 코드 완전 정답 | 0/10 | 3/10 | 0/10 |

DPO 응답 10개 중 대부분이 설명 중간이나 코드 블록 중간에서 끊김. 256 토큰 제한 안에서 설명을 충분히 길게 생성하다가 코드 자체가 미완성으로 잘려나간 패턴이 반복됨.

2차 평가 (max_new_tokens=512):

| 항목 | v4 | v5 | DPO v1 |
|---|---|---|---|
| 파라미터 정확도 | 8/10 | 10/10 | 10/10 |
| 코드 완전 정답 | 0/10 | 3/10 | 2/10 |

코드 끊김은 대부분 해소됐고 정답 수가 0에서 2로 개선됐다. 하지만 v5(3/10) 수준을 회복하지 못했다.

오답 패턴 분석:
- 함수 입력 형식을 잘못 가정 (예: gift.split('->')인데 실제 데이터는 "a b" 또는 [a, b] 형식)
- pandas, 그래프 모델링 등 문제 의도보다 복잡한 접근을 시도하다 로직이 산만해짐
- 일부 응답은 함수 정의 없이 자연어 설명만 반복하다 끝남

핵심 가설 F 검증 결과 (1차): 기각

SFT(v5) 대비 DPO 적용 모델이 코드 효율성/가독성 평가에서 높은 점수를 받을 것이라 가정했으나, 동일 10문제 비교에서 코드 완전 정답은 v5 3/10 대비 DPO 2/10으로 오히려 낮아졌다. rewards/accuracies 92.5%라는 선호도 학습 지표는 실제로 달성됐지만, 이것이 코드 정답률 개선으로 이어지지는 않았다.

---

## Phase 9 — DPO 데이터 재구성 및 재학습 (v2)

### 배경 — DPO 데이터 자체의 신뢰성 의심

DPO v1의 결과가 기대에 못 미친 원인을 찾기 위해 data/dataset_dpo.json의 chosen 코드를 직접 검토했다. 그 결과 chosen 자체가 검증되지 않은 GPT 생성 코드라는 문제를 발견했다. v1 SFT 때 겪었던 "오염된 교사 신호" 문제가 DPO 데이터에도 동일하게 존재했다.

예시 검증 결과:
- "택배 상자 꺼내기": chosen 코드가 실제 문제 로직(지그재그 적재)을 반영하지 않은 임의의 수식으로, 정답일 가능성이 낮음
- "유연근무제": chosen과 rejected 모두 검증 없이 작성된 것으로 추정되는 모호한 로직
- "지폐 접기": chosen과 rejected의 차이가 거의 없어 선호도 학습 신호로서 의미가 약함

가설: chosen을 실제로 검증된 정답(GitHub 통과 코드)으로 교체하면 DPO 성능이 개선될 것이다.

### 데이터 재구성 과정

1. data/github_solutions.json에서 228개 문제 중 190개에 GitHub 실제 통과 코드(solutions 필드) 존재 확인
2. data/rebuild_dpo_dataset.py 작성: chosen을 GitHub 코드로, rejected는 기존 DPO rejected 유지 -> 189개 생성
3. 2차 검증 — GitHub 코드도 무조건 신뢰할 수 없음을 발견: AST 기반으로 chosen 코드의 미정의 변수를 검사한 결과, 189개 중 51개(27%)가 다른 helper 함수(dfs, bfs, check 등)에 의존하는데 그 함수 본문이 추출 과정에서 누락된 "진짜 깨진 코드"였다. 38개는 단순 import 누락(예: deque, Counter, combinations)으로 복구 가능했다.
4. data/finalize_dpo_v2.py로 import 자동 보정 + 진짜 깨진 51개 제외 -> 138개 최종 데이터 (data/dataset_dpo_v2_final.json)

이는 GitHub에서 여러 함수로 나눠 짜여진 코드를 solution 함수만 잘라올 때 발생하는 문제로, v2~v3에서 겪었던 정규식 추출 버그와 본질적으로 동일한 종류의 문제였다.

### DPO v2 학습 결과

train_dpo_v2.py (MODEL_ID=v5, DPO_PATH=data/dataset_dpo_v2_final.json, 나머지 설정 v1과 동일)

| 항목 | DPO v1 | DPO v2 |
|---|---|---|
| 학습 데이터 | 204쌍 | 124쌍 |
| 학습 시간 | 54분 42초 | 1시간 6분 |
| train loss | 0.683 -> 0.587 | 0.680 -> 0.659 |
| rewards/accuracies | 0.70 -> 0.925 | 마지막 로그 0.7875 |

흥미롭게도 rewards/accuracies가 v1보다 v2에서 더 낮게 나왔다. GitHub 검증 코드(chosen)와 GPT 생성 코드(rejected) 간 스타일 차이가 v1의 "GPT가 의도적으로 다르게 생성한 쌍"보다 모호했을 가능성이 있다.

### DPO v2 vs v5 코드 품질 비교

compare_v4_v5_dpo_v2.py (v5, dpo_v2만 비교, max_new_tokens=512)

| 항목 | v5 | DPO v2 |
|---|---|---|
| 파라미터 정확도 | 10/10 | 9/10 (하락) |
| 코드 완전 정답 | 3/10 | 2/10 (DPO v1과 동일) |
| hint 응답 시간 | 28,231ms | 75,159ms (4배 가까이 느려짐) |
| solution 응답 시간 | 20,784ms | 83,568ms (4배 가까이 느려짐) |

오답 패턴은 v1과 거의 동일하게 반복: 입력 형식 잘못 가정(record.split('->')인데 실제 데이터는 "a b" 형식), 과도하게 복잡한 접근(불필요한 DP, 그래프 모델링), 코드 미완성.

핵심 가설 G 검증 결과: 기각

chosen 데이터 품질을 GPT 생성 코드에서 GitHub 검증 코드로 교체했음에도 코드 정답률은 동일(2/10)했고, 응답 시간은 오히려 4배 가까이 느려졌으며 파라미터 정확도도 소폭 하락했다. 이는 데이터 품질이 DPO 성능 저하의 주된 원인이 아니었음을 시사한다.

### 근본 원인 재분석

데이터 품질을 두 가지 방식(GPT 생성 vs GitHub 검증)으로 다르게 줬는데도 동일한 결과(2/10, 응답시간 급증)가 반복됐다는 것은, 문제가 데이터가 아니라 DPO라는 학습 방식과 현재 프로젝트 조건의 근본적인 부정합에 있다는 결론으로 이어졌다.

1. 베이스 모델(v5) 자체의 코드 생성 능력 부족 (가장 근본적인 원인으로 합의): v5는 10문제 중 3개(30%)만 정답을 맞히는 수준이다. DPO는 "이미 가진 능력 중에서 더 나은 것을 선택하게" 학습시키는 기법이지, "없는 능력을 새로 만들어내는" 기법이 아니다. 베이스 모델의 코드 생성 능력 자체가 부족한 상태에서 선호도만 조정하니, 모델이 가진 적은 역량이 코드 정확성보다 "그럴듯한 설명 늘리기" 같은 다른 방향으로 쏠린 것으로 추정된다.
2. 7B 모델 + 4bit 양자화의 근본적인 표현력 제약.
3. 학습 데이터 규모(228개 문제)가 코딩 능력의 일반화에 필요한 양에 비해 작다.

응답 시간이 두 차례 모두 일관되게 2~4배 느려진 것도 단서다. 데이터를 다르게 줘도 같은 부작용이 재현된다는 것은, beta 값이나 학습 스텝 수 같은 DPO 자체의 하이퍼파라미터 설정이 영향을 줬을 가능성도 배제할 수 없으나, 두 가지 다른 데이터셋에서 같은 패턴이 반복된 점을 고려하면 베이스 모델의 능력 한계가 가장 설명력 있는 가설이다.

### 결론 및 다음 방향

DPO를 추가로 개선(RPO 적용, 데이터 추가 확장)하기보다, DPO 적용 전에 베이스 모델(SFT)의 코드 생성 능력 자체를 끌어올리는 것이 더 근본적인 해결책이라고 판단했다. 이에 Phase 10에서는 LoRA rank 증대를 먼저 시도한다.

---

## Phase 10 — LoRA rank 증대 시도 (v6)

### 배경

Phase 9의 결론에 따라, DPO보다 SFT 모델(v5)의 근본적인 코드 생성 능력을 올리는 것을 우선하기로 했다. SFT는 "무엇이 정답인가"를 직접 학습시키는 방식이고, DPO는 "이미 어느 정도 정답을 만들 수 있는 모델"에 적용해야 효과적이다. v5가 아직 정답률 30% 수준에 머물러 있어 DPO를 적용하기엔 시기가 일렀다는 것이 Phase 9의 결론이었다.

### 시도 옵션 검토

1. LoRA rank 16 -> 32 증대 (표현력 증가)
2. epoch 수 증대
3. 학습 데이터 규모 확장 (IFD 선별 비율 40% -> 60%, 또는 원본 문제 수 증가)

"한 번에 한 변수만 변경" 원칙에 따라, 이번엔 LoRA rank만 32로 변경하고 나머지(데이터, learning rate, epoch 등)는 v5와 완전히 동일하게 유지하기로 했다.

### 시행착오 — 통제 변수 오염 발견 및 수정
v6 학습 코드를 처음 작성할 때 rank만 변경하려 했으나, 실수로 MAX_LENGTH(512->256)와 num_train_epochs(3->1)까지 함께 바뀐 상태로 학습이 진행됐다. 학습이 1시간 22분 만에 비정상적으로 빨리 끝나는 것을 보고 fc(파일 비교) 명령으로 원본 train.py와 대조해 변수 오염을 발견했다. MAX_LENGTH=512, epoch=3으로 원복 후 재학습했다.

### v6 설정 및 결과

| 항목 | v5 | v6 |
|---|---|---|
| LoRA r | 16 | 32 |
| LoRA alpha | 32 | 64 |
| 학습 가능 파라미터 | 40,370,176 (0.53%) | 80,740,352 (1.05%) |
| 학습 데이터 | 1,381 (동일) | 1,381 (동일) |
| eval loss (최종) | 0.222 | 0.2096 (개선) |
| 학습 시간 | 34시간 18분 | 8시간 38분 |

### v5 vs v6 코드 품질 비교 평가 (동일 10문제, max_new_tokens=512)

| 항목 | v5 | v6 |
|---|---|---|
| 파라미터 정확도 | 10/10 | 10/10 |
| 코드 완전 정답 | 3/10 | 2/10 (하락) |
| hint 응답 시간 | 55,426ms | 240,227ms (4.3배 증가) |
| solution 응답 시간 | 26,378ms | 159,721ms (6.1배 증가) |

eval loss는 v5보다 개선됐음에도 코드 정답률은 하락했고, 응답 시간은 4~6배 급증했다. 특히 9번 문제(퍼즐 게임 챌린지)에서는 동일한 함수 코드를 주석 처리한 채로 6~7번 반복해서 찍어내는 비정상적인 생성 패턴이 발견됐다.

핵심 가설 H 검증 결과: 기각

LoRA rank를 늘려 표현력을 높이면 코드 생성 능력이 개선될 것이라 가정했으나, eval loss는 개선됐음에도 실제 코드 정답률은 v5보다 하락했고 응답 시간과 생성 안정성(반복 생성 버그) 모두 악화됐다. DPO에서 확인한 "지표 개선과 다운스트림 성능 개선은 별개"라는 패턴이 SFT 하이퍼파라미터 조정에서도 동일하게 재현됐다.

---

## Phase 11 — 학습 데이터 검증 및 정제 시도 (v7)

### 배경
DPO 데이터에서 GitHub 코드 27%가 helper 함수 의존으로 단독 실행 불가능했던 것을 발견한 경험을 SFT 학습 데이터(train_selected.jsonl, 1,381개)에도 적용해, 동일한 문제가 있는지 검증했다.

### 검증 결과

AST 기반으로 solution 타입 데이터(973개)의 미정의 변수를 검사한 결과:

| 분류 | 개수 | 비율 |
|---|---|---|
| 정상 | 871 | 89.5% |
| import 추가로 해결 가능 | 75 (보정 후) | - |
| 진짜 깨진 코드(helper 함수 의존) | 27 | 약 2.8% |
| 문법 오류 | 3 | - |

DPO 데이터(27%)보다 훨씬 낮은 비율(약 2.8%)이지만, 일부 문제(소수 찾기 6회, 주차 요금 계산 4회, 양궁대회 3회 반복)에서 같은 패턴의 깨진 코드가 여러 변형(Evol-Instruct 산출물)에 걸쳐 나타났다. data/finalize_sft_data.py로 import 보정 및 깨진 데이터 제외 후 1,354개로 정제했다(data/train_selected_v2.jsonl).

### v7 설정 및 결과

LoRA rank는 v5와 동일하게 16으로 유지하고, 학습 데이터만 정제본으로 교체했다(한 번에 한 변수만 변경 원칙 준수).

| 항목 | v5 | v7 |
|---|---|---|
| 학습 데이터 | 1,381 | 1,354 (27개 제외, 75개 import 보정) |
| LoRA r | 16 (동일) | 16 (동일) |
| eval loss (최종) | 0.222 | 0.2298 (오히려 악화) |
| 학습 시간 | 34시간 18분 | 5시간 38분 |

### v5 vs v7 비교 평가 — 중단

compare_v5_v7.py로 평가를 시작했으나, v7의 응답 시간이 v5 대비 동일하게 4~6배 급증하는 현상이 재현됐다(예: hint 응답 시간이 v5 70초대에서 v7 250~450초대로 증가). 이는 v6에서 관찰된 패턴과 동일했다. 코드 정답률 확인 전 평가를 중단했다.

핵심 가설 I 검증 결과: 시도 중단 (가능성 낮음으로 판단)

학습 데이터 정제는 eval loss를 오히려 악화시켰고, 응답 시간 폭증이라는 동일한 부작용이 또다시 나타나 코드 정답률 확인 전에 중단했다.

### 세 가지 독립적 시도에서 공통적으로 나타난 패턴

| 시도 | 바꾼 변수 | eval loss / 선호도 지표 | 코드 정답률 | 응답 시간 |
|---|---|---|---|---|
| DPO v1/v2 | 선호도 학습 추가 | rewards/accuracies 개선 | 3->2/10 하락 | 4배 증가 |
| v6 | LoRA rank 16->32 | eval loss 개선 (0.222->0.2096) | 3->2/10 하락 | 4~6배 증가 |
| v7 | 학습 데이터 정제 | eval loss 악화 (0.222->0.2298) | 확인 전 중단 | 4~6배 증가 (v6과 동일) |

세 가지 서로 다른 변수(선호도 학습, LoRA rank, 데이터 품질)를 독립적으로 바꿨음에도, 매번 같은 부작용(응답 시간 급증, 코드 정답률 하락 또는 정체)이 나타났다. 이는 개별 변경 자체가 원인이 아니라, v5의 학습 레시피(MAX_LENGTH=512 + 3epoch 조합)가 EOS 토큰 학습이 불안정한 경계에 있고, 거기서 무엇을 조금만 바꿔도 그 불안정성이 악화되는 방향으로 작용한다는 더 근본적인 가설로 이어진다. 이 가설은 학습 시간 자체가 v5(34시간 18분)와 v6(8시간 38분), v7(5시간 38분) 사이에 4~6배 차이가 나는 미확인 이상 현상과도 연결되어 있을 가능성이 있으나, 시간 제약상 직접 검증(예: MAX_LENGTH를 낮춰 동일 데이터로 재학습)은 진행하지 못했다. 이는 명확한 한계로 남긴다.

### 결론 및 다음 방향

rank 조정(v6)과 데이터 품질 정제(v7) 모두 v5를 넘지 못했다. 남은 가장 직접적인 미시도 변수는 학습 데이터의 규모 자체였다. v1~v7 전체에서 데이터 양을 의도적으로 늘리는 시도는 한 번도 통제된 방식으로 진행한 적이 없었다(Evol-Instruct는 변형이지 원본 문제 자체의 확장은 아니었다). 이에 Phase 12에서는 원본 문제 풀 자체를 확장하는 시도를 진행한다.

---

## Phase 12 — 학습 데이터 규모 확장 (v8, 진행 중)

### 배경

Phase 10, 11에서 LoRA rank와 데이터 품질을 각각 바꿔봤지만 둘 다 v5를 넘지 못했다. 남은 가장 직접적인 변수는 학습에 쓰이는 원본 문제 풀의 규모였다. 기존에는 프로그래머스 Level 1~2 문제(228개)만 사용했는데, 이번에는 Level 3까지 범위를 넓혀 원본 문제 수 자체를 늘리는 시도를 진행한다.

### 변경 범위

엄밀하게는 "데이터 양 하나만" 바뀌는 것은 아니다. Level 3을 추가하면서 문제 난이도 분포도 함께 넓어진다. 이는 "학습 데이터 규모 확장"이라는 하나의 의도된 변경으로 묶어 다루고, 이 점을 한계로 명시한다.

### 데이터 파이프라인 재실행

1. crawl_problems.py: LEVELS를 [1, 2]에서 [1, 2, 3]으로 변경 후 재수집. 274개 -> 383개 URL. Level 1+2는 기존과 동일(227개, SQL 46개 별도)하고 Level 3에서 109개가 신규 추가됨을 확인.
2. fetch_problems.py: SQL 필터링을 거쳐 320개 본문 파싱 완료(기존 228개에서 92개 증가).
3. collect_github.py: GitHub 정답 코드 매칭, 251개 문제에서 풀이 확보(총 2,808개 코드).
4. GitHub 코드 AST 검증(신규 단계): Phase 11에서 만든 검증 로직을 GitHub 수집 코드 전체(2,808개)에 적용. 정상 67.4%(1,893개), import 보정 18.2%(511개), 진짜 깨진 코드 14.2%(398개, 문법 오류 0.2% 포함). 검증 결과 풀이가 남은 문제는 243개(기존 190개 대비 28% 증가)로, data/github_solutions_v2.json에 저장. Level 3 문제가 그래프/DP 등 복잡한 알고리즘을 다뤄 helper 함수 의존 코드가 더 많았던 것으로 추정된다.
5. generate_dataset.py: 신규 92개 문제에 대해 GPT-4o-mini로 힌트/접근법/정답 생성(이미 처리된 제목은 자동 스킵). 320개 완료.
6. build_dataset.py: 검증된 github_solutions_v2.json 기준으로 재실행 -> 힌트 321, 접근법 321, 정답코드 2,127개(검증 전 2,483개에서 356개 추가 감소, 품질 정제 효과), 총합 2,769개(data/dataset_v3.json).
7. convert_to_jsonl.py: train 2,492 / val 277로 분리. 기존 v5 시절 val.jsonl은 data/val_v5.jsonl로 백업해 eval loss 직접 비교가 불가능해진 점을 기록으로 남김.
8. evol_dataset.py: 신규 92개 문제에 대해서만 Evol-Instruct 변형 생성(기존 228개는 스킵). constraint/scale/recursive 각 640개씩 총 1,920개 변형 샘플 생성.
9. merge_dataset.py: dataset_v3.json(2,769) + dataset_evol.json(1,920) = 4,689개. train 4,220 / val 469.
10. ifd_select.py: 진행 예정. v5와 동일하게 N_CLUSTERS=10, SAMPLE_RATE=0.40, ADAPTER_PATH=v4 모델 유지.

### 단계별 비교

| 항목 | v5 (228개 원본) | v8 (320개 원본) |
|---|---|---|
| 원본 문제 수 | 228 | 320 (+40%) |
| GitHub 정답 매칭 문제 | 190 | 243 (+28%, 검증 후) |
| build_dataset 결과 | 681 (v1 시절 기준) | 2,769 |
| Evol-Instruct 변형 | 1,362 | 1,920 |
| 병합 후 (선별 전) | 3,848 | 4,689 (+22%) |
| IFD 선별 후 (최종 학습 데이터) | 1,381 | 진행 예정 |

### 검증 단계 추가에 대한 메모

이번 시도에서는 Phase 11에서 배운 교훈(GitHub 코드도 무조건 신뢰할 수 없다)을 데이터 파이프라인에 정식으로 통합했다. 기존 v5는 이 검증 없이 진행됐었는데, 이번 v8 데이터에는 GitHub 코드 검증이 추가로 들어갔다는 점도 v5와 완전히 동일한 조건은 아니라는 한계로 남는다. 다만 이는 데이터 품질을 낮추는 방향이 아니라 높이는 방향의 차이이므로, v8의 결과가 v5보다 나빠진다면 "데이터 규모 확장 자체"의 효과를 의심할 근거가 되고, 반대로 좋아진다면 규모 확장과 품질 개선이 함께 기여했다고 해석한다.

### 다음 단계

ifd_select.py 완료 후 v8 학습(train.py 기반, LoRA r=16, MAX_LENGTH=512, epoch=3 등 v5와 완전히 동일한 설정 유지) -> v5 vs v8 비교 평가(compare_v4_v5.py 패턴 재사용) 진행.

### v8 학습 및 1차 평가(10문제) 결과

IFD 선별 결과 1,381개(v5) -> 1,685개(v8, 39.9% 동일 비율). train_v8.py로 학습(LoRA r=16, MAX_LENGTH=512, epoch=3, v5와 완전히 동일한 설정).

| 항목 | v5 | v8 |
|---|---|---|
| 학습 데이터 | 1,381 | 1,685 (+22%) |
| eval loss (최종) | 0.222 | 0.2343 (악화) |
| 학습 시간 | 34시간 18분 | 8시간 32분 |

10문제 비교 평가(compare_v5_v8.py) 결과: 코드 완전 정답 v5 3/10, v8 2/10. v6, v7과 동일한 패턴(eval loss 악화/개선과 무관하게 코드 정답률 하락, 응답 시간 v5 대비 2.5배 증가)이 또 반복됐다.

### repetition_penalty 검증 (추론 설정 vs 학습 자체의 문제 분리 시도)

v6, v7, v8 모두 응답이 길어지고 동일 코드를 반복 생성하는 공통 증상을 보였다. 이것이 학습 결과 자체의 문제인지, 추론 시 생성 설정의 문제인지 분리하기 위해 v8에 repetition_penalty=1.3을 적용해 10문제를 재평가했다(compare_v8_reppenalty.py).

| 항목 | v8 (penalty 없음) | v8 (penalty=1.3) |
|---|---|---|
| hint 응답 시간 | 112,066ms | 33,044ms (v5 수준으로 회복) |
| solution 응답 시간 | 108,776ms | 28,669ms (v5 수준으로 회복) |
| 코드 완전 정답 | 2/10 | 0/10 (오히려 악화) |
| 파라미터 정확도 | 9/10 | 9/10 |

응답 시간은 v5 수준으로 빨라졌으나, 코드 자체가 와해되는 현상(미완성 함수, 의미 없는 변수 사용, 함수 시그니처 불일치)이 나타나 코드 품질은 오히려 더 나빠졌다. repetition_penalty가 코드에 자연스럽게 반복되는 키워드(def, return, 변수명)까지 억제하면서 부작용을 일으킨 것으로 추정된다. 이로써 "추론 설정만 조정하면 v5 수준을 회복할 수 있다"는 가설은 기각됐고, 문제가 추론 설정이 아니라 더 근본적인 곳에 있다는 쪽으로 무게가 옮겨갔다.

---

## Phase 13 — v5 재현 실험 및 30문제 확장 평가

### 배경 — "v5가 우연히 좋게 나온 결과는 아닐까?"

DPO, v6, v7, v8, repetition_penalty까지 다섯 차례의 독립적 시도가 모두 v5(코드 정답 3/10)를 넘지 못했다. 이 시점에서 두 가지 상반된 가설이 동시에 성립 가능했다:

1. v5의 학습 설정(데이터, rank, MAX_LENGTH, epoch) 자체가 이 환경에서 안정적으로 가장 좋은 결과를 낸다.
2. v5는 1,381개라는 적은 데이터로 학습한 7B 모델의 결과이며, 단일 실험의 우연한 변동(분산)일 뿐, 다시 학습해도 같은 결과가 재현되지 않을 수 있다.

이를 구분하기 위해, v5와 완전히 동일한 데이터·설정으로 처음부터 다시 학습시키는 재현 실험을 진행했다.

### v5 원본 데이터 git 복원

train_selected.jsonl과 val.jsonl은 v8 작업 과정에서 이미 덮어써진 상태였다. git 커밋 fd2e98f("누락된 핵심 학습/평가 데이터 파일 추가")에 v5 학습 직후 시점의 원본이 그대로 보존되어 있음을 확인했다(train_selected.jsonl | 1381 ++, val.jsonl | 385 +). git show fd2e98f:data/train_selected.jsonl로 1,381개 원본을, git show fd2e98f:data/val.jsonl로 385개 원본을 각각 train_selected_v5_original.jsonl, val_v5_original.jsonl로 복원했다.

### v5-repro 학습 결과

train_v5_repro.py(MODEL_ID, LoRA r=16, MAX_LENGTH=512, epoch=3 등 train.py와 완전히 동일, 데이터 경로만 복원된 원본으로 지정)로 재학습했다.

| 항목 | v5 (원본) | v5-repro |
|---|---|---|
| 학습 데이터 | 1,381 (동일) | 1,381 (동일, git 복원) |
| eval loss (최종) | 0.222 | 0.2229 (거의 일치) |
| 학습 시간 | 34시간 18분 | 7시간 56분 |

eval loss가 소수점 셋째 자리까지 거의 일치했다. 이는 동일한 데이터·설정으로 재학습하면 결과가 안정적으로 재현된다는 강력한 증거였다. 동시에 학습 시간은 또 한 번 v5 원본(34시간)보다 훨씬 짧게(8시간) 나와, 학습 시간 차이가 학습 코드나 데이터의 문제가 아니라 그날그날의 시스템 환경(추정: RAM 스왑 등 외부 요인)에 좌우된다는 가설을 다시 한번 뒷받침했다.

### 10문제 평가의 한계 인식과 30문제 확장

v5-repro의 eval loss가 v5와 거의 같다는 것을 확인했지만, 동시에 "v5와 v6/v7/v8 사이의 코드 정답률 차이(3/10 vs 2/10)가 통계적으로 의미 있는 차이인지"에 대한 근본적인 의문이 제기됐다. 10문제 표본에서 1개 차이는 우연으로도 충분히 설명 가능한 수준이다.

이에 평가셋을 10문제에서 30문제로 확장했다(기존 10문제 + Level 1 신규 10문제 + Level 2 신규 10문제, select_30_problems.py로 선정). 이미 학습이 완료된 v4, v5, dpo, v8 네 모델을 동일한 30문제로 재평가했다(compare_30problems.py).

### 30문제 평가 결과 — 10문제 평가와 다른 결론

| 모델 | 코드 완전 정답 (30문제 기준) | 비율 |
|---|---|---|
| v4 | 9/30 | 30% |
| v5 | 17/30 | 57% |
| dpo | 13/30 | 43% |
| v8 | 16/30 | 53% |

10문제 평가에서는 v5(3/10=30%)와 v8(2/10=20%)이 10%p 차이로 꽤 다른 모델처럼 보였으나, 30문제로 확장하니 v5(57%)와 v8(53%)의 차이는 4%p로 줄어 통계적으로 거의 구분되지 않는 수준이 됐다. v4(30%)는 30문제에서도 여전히 명확하게 낮아, 이 차이는 표본 크기와 무관하게 실재하는 것으로 보였다. dpo(43%)도 v4보다는 높지만 v5/v8보다는 낮은 중간 위치를 유지했다.

핵심 가설 J 검증 결과: 부분 기각

"v8(데이터 규모 확장)이 v5보다 코드 생성 능력이 떨어진다"는 이전 결론(10문제 기준)은 표본 크기의 한계에서 비롯된 과대해석이었을 가능성이 높다. 30문제 기준으로는 v5와 v8이 사실상 동급 성능이다. 다만 v4와 v5/v8/dpo 사이의 차이는 30문제에서도 유지되어, v1~v4에서 v5로 가는 구간의 개선(논문 기반 데이터 선별)은 표본 크기와 무관하게 실재하는 효과로 판단된다.

### 결론

1. v5는 재현 가능하다 (v5-repro의 eval loss가 거의 일치).
2. 그러나 v5와 v6/v7/v8 사이의 "코드 정답률 차이"는 상당 부분이 10문제라는 작은 평가셋의 통계적 노이즈였다. 30문제로 넓히자 v5와 v8의 차이는 거의 사라졌다.
3. v6(rank 증대)과 v7(데이터 정제)에 대해서는 30문제 재평가를 진행하지 않았으므로, 이 두 시도가 정말로 v5보다 못한지 혹은 마찬가지로 표본 노이즈였는지는 확정할 수 없다는 한계가 남는다.
4. 응답 시간 급증과 반복 생성 버그(v6, v7, v8에서 공통 관찰)는 30문제 평가에서도 v5보다 느린 경향이 유지됐다(v8 hint 평균 34초 vs v5 47초로 오히려 v8이 빠르게 측정된 회차도 있어, 이 지표는 실행 시점의 시스템 상태에 따라 흔들리는 것으로 보인다).

### 종합 판단

v5는 최종 모델로서 충분히 신뢰할 수 있는 선택이며, v8(데이터 규모 확장) 역시 30문제 기준으로는 거의 동등한 성능을 보여 "데이터 규모 확장이 무의미하지 않았다"는 쪽으로 재평가됐다. 다만 시간 제약상 v6/v7의 30문제 재검증, 그리고 학습 데이터 자체의 실행 기반 검증(examples 필드가 처음부터 수집되지 않아 시도하지 못함)은 향후 과제로 남는다.

---

## Phase 14 — 경량 데이터 정제(중복 제거) 및 v9 학습, 최종 종합 평가

### 배경 — ifd_select.py 재실행 중 발견한 데이터 이상

v8 데이터 파이프라인의 일부 단계(evol_dataset.py 등)를 재실행하는 과정에서, 같은 입력으로도 train.jsonl의 크기가 4,220개에서 4,011개로 줄어든 것을 발견했다. 원인을 추적한 결과, Evol-Instruct 변형 데이터 일부에 동일한 코드 블록을 주석으로 반복 생성하는 패턴이 섞여 있었고, 이를 별도로 정리하는 과정에서 데이터 일부가 함께 줄어든 것으로 파악됐다. 이 변경 자체는 의도된 것은 아니었으나, 결과적으로 더 정제된 데이터(4,011개)가 만들어졌다.

이 4,011개를 ifd_select.py로 선별한 결과 1,601개(39.9%, 비율 동일)가 나왔다.

### 데이터 추가 정제 — 중복 제거와 print문 정리

1,601개를 직접 검토하는 과정에서 두 가지 문제를 발견했다:

1. 같은 제목의 문제에 대해 GitHub에서 가져온 여러 풀이가 모두 학습 데이터에 중복 포함되어 있었다(예: "N으로 표현"이 서로 다른 코드로 2번 등장).
2. 일부 solution 코드에 `print()` 디버깅 라인이 그대로 남아있었다.

`scan_patterns.py`로 정량화한 결과:

| 패턴 | 개수 | 비율 |
|---|---|---|
| print() 포함 | 46개 | 4.3% |
| 주석 5줄 이상(반복 의심) | 49개 | 4.6% |
| 3줄 미만(너무 짧음) | 10개 | 0.9% |
| 60줄 초과(너무 길음) | 6개 | 0.6% |

`data/clean_train_selected.py`로 경량 정제(print 라인 제거 + 같은 제목 solution 중복 제거, 가장 먼저 등장한 것만 유지)를 적용했다.

| 항목 | 정제 전(1,601개) | 정제 후(1,278개) |
|---|---|---|
| print() 라인 제거 | - | 11개 |
| 중복 제목으로 제외 | - | 323개 |
| solution 개수 | 1,073 | 750 |

중복 제거 비중(323개, 약 30%)이 print 정리(11개)보다 훨씬 컸다. 즉 원본 데이터에는 인기 있는 쉬운 문제일수록 GitHub 풀이가 여러 개 중복 포함되어 있었고, 이로 인해 데이터의 "양"은 많아 보였지만 실제 "고유 문제 다양성"은 그보다 작았다는 것을 의미한다.

### v9 학습 결과

train_v9.py(LoRA r=16, MAX_LENGTH=512, epoch=3 등 v5~v8과 완전히 동일, 데이터만 1,278개 정제본으로 교체)로 학습했다.

| 항목 | v5 | v8 | v9 |
|---|---|---|---|
| 학습 데이터 | 1,381 | 1,685 | 1,278 (중복제거+정제) |
| eval loss (최종) | 0.222 | 0.2343 | 0.262 (가장 악화) |
| 학습 시간 | 34시간 18분 | 8시간 32분 | 6시간 11분 |

eval loss는 데이터 수가 가장 적은 v9에서 가장 나쁘게 나왔다. 다만 이전 패턴(eval loss와 코드 정답률이 반드시 같이 움직이지 않는다)을 고려해, 결론을 내리기 전에 평가를 진행했다.

### v5 vs v8 vs v9 — 30문제 동시 비교

`compare_v5_v8_v9_30problems.py`로 동일 30문제 기준 세 모델을 비교 평가했다.

| 항목 | v5 | v8 | v9 |
|---|---|---|---|
| hint 평균 응답시간 | 37,951ms | 58,472ms | 84,092ms |
| solution 평균 응답시간 | 44,129ms | 58,064ms | 63,768ms |
| 파라미터 정확도 | 29/30 | 29/30 | 29/30 |
| 코드 완전 정답 | 14/30 (47%) | 13/30 (43%) | 14/30 (47%) |

응답 시간은 v5 < v8 < v9 순으로 점점 느려지는 경향이 뚜렷했다(v9는 30문제 중 11개에서 max_new_tokens=512를 거의 다 채우는 109초대 응답을 보임). eval loss가 가장 나빴던 v9가 응답 시간도 가장 느렸다는 점은 일관성이 있다.

다만 코드 완전 정답률은 v5 47%, v8 43%, v9 47%로 **세 모델이 거의 동급**이었다. 이번 30문제 채점에서는 이전 Phase 13의 30문제 평가(v5 57%, v8 53%)보다 전체적으로 낮은 수치가 나왔는데, 이는 채점 엄격도 차이와 temperature=0.3 샘플링에 따른 실행 간 변동성으로 추정된다. 즉 같은 모델이라도 평가를 다시 실행하면 수 %p 정도의 차이가 자연스럽게 발생할 수 있다는 것을 시사한다.

핵심 가설 K 검증 결과: 기각

학습 데이터의 중복을 제거하고 품질을 정제하면(v9) v5나 v8보다 코드 정답률이 개선될 것이라 가정했으나, 실제로는 v5와 동률(47%)에 머물렀고 eval loss와 응답 시간은 오히려 가장 나빴다. 데이터 정제 자체가 해롭지는 않았지만, 이 정도 규모(7B 모델, 1,300~1,700개 데이터)에서 추가적인 개선을 만들어내지는 못했다.

### 최종 종합 결론 — v5, v8, v9는 통계적으로 동급

지금까지 시도한 6가지 변형(DPO, v6, v7, v8, v9, repetition_penalty)을 모두 종합하면:

| 시도 | 변경한 것 | 30문제 기준 결과 |
|---|---|---|
| v5 (기준) | - | 47~57% |
| v8 | 데이터 규모 확장(228→320개 문제) | 43~53% |
| v9 | v8 데이터 중복 제거 + 정제 | 47% |
| v6, v7 | rank 증대 / 데이터 정제 (10문제만 평가) | 10문제 기준 v5보다 낮음, 30문제 재검증 안 함 |
| DPO | 선호도 학습 추가 | 43% |

v5, v8, v9는 30문제 기준으로 모두 43~57% 범위에서 겹치며, 이는 통계적으로 뚜렷하게 구분되는 차이가 아니다. 이는 **현재 조건(Qwen2.5-Coder-7B, QLoRA r=16, 4bit 양자화, 1,300~1,700개 학습 샘플, RTX 3060)에서는 데이터 양을 조금 늘리거나 줄이거나, 품질을 정제하거나, 선호도 학습을 추가하는 정도의 변경으로는 코드 생성 성능이 40~55% 구간을 벗어나지 못한다**는 것을 시사한다. 이 구간을 벗어나려면 모델 크기, 데이터 규모의 차수(10배 이상), 또는 코드 실행 기반 검증처럼 더 근본적인 변화가 필요하다고 판단된다.

이에 따라 **v5를 최종 배포 모델로 유지**하기로 결정했다. v8, v9는 "데이터 규모/품질을 바꿔도 성능이 유지된다"는 안정성 확인 차원의 의미 있는 기록으로 남기고, 추가 학습 시도는 이 프로젝트의 범위에서 마무리한다.

---

## 핵심 발견 (회고)

1. train/val loss는 실제 품질을 보장하지 않는다. v1은 loss 0.552로 잘 학습된 듯 보였으나 출력은 사용 불가 수준이었다.

2. 데이터 품질 > 데이터 양. v1->v2에서 양을 4배 늘렸으나 품질 문제로 개선 없었고, 정규식 한 줄 고친 v3에서 비로소 개선됐다.

3. 프롬프트 설계가 학습 효율에 결정적이다. v3->v4에서 함수 시그니처 한 줄로 val loss 23% 감소 + 파라미터 오류 완전 해결.

4. 하드웨어 한계를 명확히 인식하고 적절한 시점에 전환한다. 무작정 데이터를 늘리는 대신 논문 기반 선별 전략으로 방향 전환.

5. 태스크별로 실용성을 분리해 판단한다. 코드 생성은 GPT 대체 실패했으나 힌트 태스크는 속도/비용 면에서 실용 가능했다.

6. "한 번에 한 변수만 변경" 원칙은 실제로 효과가 있다. 각 변경의 효과를 분리 측정할 수 있었다. 다만 v6 학습 코드 작성 중 이 원칙을 실수로 어긴 적이 있었고(MAX_LENGTH, epoch까지 같이 변경), 파일 비교로 발견해 즉시 수정했다.

7. 논문 방법론을 제한된 환경에 맞게 적용하는 능력. A100 기준 논문을 RTX 3060에서 재현하기 위해 4bit 양자화, gradient checkpointing 등 환경 최적화를 직접 해결.

8. 라이브러리 버전 호환성 문제도 실험의 일부다. trl 0.8.6~0.9.6에서 4bit 모델과의 충돌을 디버깅하며 0.11.4로 해결. 환경 분리(dpo_env)로 기존 파이프라인 보호.

9. 선호도 학습 지표(rewards/accuracies)와 다운스트림 태스크 성능(pass@1)은 별개다. DPO가 선호도 학습 지표에서 92.5%를 달성했음에도 실제 코드 정답률은 v5보다 낮아졌다.

10. 같은 결과가 다른 조건에서 반복되면, 그 조건이 아니라 더 근본적인 원인을 봐야 한다. DPO 데이터를 GPT 생성 코드에서 GitHub 검증 코드로 바꿔도 결과(2/10, 응답시간 급증)가 동일했고, LoRA rank를 바꿔도, 학습 데이터를 정제해도 같은 부작용이 반복됐다. 세 가지 독립적인 변수에서 같은 증상이 나타난다는 것은 그 변수들 자체가 원인이 아니라는 강한 신호다. 이를 무시하지 않고 "남아있는 미시도 변수(데이터 규모)로 넘어간다"는 판단으로 이어졌다.

11. 검증 로직은 한 번 만들면 재사용할 수 있는 자산이 된다. DPO 데이터 검증에서 만든 AST 기반 미정의 변수 검사 로직을, SFT 데이터 검증(v7)과 v8의 GitHub 코드 검증에도 그대로 재사용했다. 매번 새로 만들지 않고 점검 도구를 축적해 나가는 것이 데이터 파이프라인을 다루는 효율적인 방식이었다.

12. 작은 평가셋(10문제)에서의 1~2개 차이는 통계적으로 거의 의미가 없을 수 있다. v5와 v8의 코드 정답률 차이가 10문제 기준 3/10 vs 2/10(10%p)으로 꽤 크게 보였으나, 30문제로 확장하자 57% vs 53%(4%p)로 차이가 거의 사라졌다. "v5가 v6/v7/v8보다 우월하다"는 그동안의 반복된 결론 중 상당 부분이 표본 크기의 한계에서 비롯된 과대해석이었을 가능성을 인정해야 했다. 다만 v4와 v5 사이의 차이는 30문제에서도 유지되어, 모든 차이가 노이즈는 아니었다는 점도 함께 확인했다. 평가 결과를 해석할 때는 표본 크기를 항상 함께 고려해야 한다는 교훈을 얻었다.

13. git은 단순 버전 관리 도구가 아니라 데이터 복원 도구로도 쓸 수 있다. train_selected.jsonl이 v8 작업 중 덮어써져 v5의 원본 데이터가 로컬에는 더 이상 존재하지 않았지만, 과거 커밋(fd2e98f)에 보존되어 있어 git show로 정확히 복원할 수 있었다. 평소 데이터 변경 시점마다 커밋을 남겨두는 습관이 이런 재현 실험을 가능하게 만든 핵심 안전장치였다.

14. 데이터의 "양"과 "다양성"은 다른 개념이다. v8의 학습 데이터(1,685개)에는 인기 있는 쉬운 문제의 GitHub 풀이가 여러 개씩 중복 포함되어 있었고, 중복을 제거하니 실제 고유 문제 수는 30% 줄었다(1,073→750개 solution). 샘플 수가 많다고 해서 모델이 그만큼 더 다양한 패턴을 배우는 것은 아니라는 것을 직접 확인했다. 다만 이 중복을 제거한 v9도 v5/v8과 동급 성능에 그쳐, 이번 경우엔 다양성 문제가 성능의 결정적 제약은 아니었다는 것도 함께 알게 됐다.

15. 같은 모델을 평가해도 실행마다 결과가 달라질 수 있다(temperature 샘플링, 채점자의 엄격도 차이). Phase 13의 30문제 평가(v5 57%, v8 53%)와 Phase 14의 30문제 평가(v5 47%, v8 43%)는 같은 모델, 같은 평가셋임에도 약 10%p씩 다르게 나왔다. 절대적인 숫자 하나에 매달리기보다, 여러 모델 간의 "상대적 순서와 격차"를 보는 것이 더 안정적인 결론으로 이어진다는 것을 알게 됐다.

---

## 트러블슈팅 요약

| 문제 | 해결 |
|---|---|
| Windows cp949 인코딩 에러 (trl jinja 로드 실패) | .read_text()에 encoding="utf-8" 강제 패치 (patch.py) |
| RTX 3060 BFloat16 미지원 | fp16=False, bf16=False로 mixed precision 비활성화 |
| GitHub API 응답 타입 불일치 | 응답이 list면 건너뛰는 가드 추가 |
| 정규식 함수 추출 버그 (v2 실패 원인) | 들여쓰기 기반 함수 범위 판단으로 교체 |
| 파라미터 오류 (v3 실패 원인) | 학습 프롬프트에 함수 시그니처 명시 |
| 추론 시 vLLM Windows 미지원 | transformers 직접 로드로 전환 |
| peft 버전 불일치 (alora_invocation_tokens 등) | adapter_config.json에서 신버전 전용 키 제거 |
| sentence-transformers 버전 충돌 (transformers 5.x) | sentence-transformers==2.7.0 + transformers==4.40.0 다운그레이드 |
| 4bit 모델 OOM (IFD 계산 시) | accelerate==0.27.2 다운그레이드 |
| gradient checkpointing + 4bit 충돌 | prepare_model_for_kbit_training() 적용 |
| DPO NoneType 오류 (trl 0.8.6~0.9.6) | trl==0.11.4로 업그레이드 + dpo_env 분리 |
| DPO 텐서 크기 불일치 (trl utils.py) | trl 버전 업그레이드로 근본 해결 |
| DPO 추론 시 코드 중간 끊김 (256토큰) | max_new_tokens=512로 확대해 끊김 해소, 단 v5 수준 정답률은 회복 못함 |
| token accuracy 측정 코드 부재 | compute_token_accuracy.py 신규 작성으로 재현 가능한 측정 체계 구축 |
| DPO chosen 데이터가 검증되지 않은 GPT 생성 코드 | GitHub 통과 코드로 교체 시도(v2) -> 결과 동일, 데이터 품질이 주원인이 아님을 확인 |
| GitHub 코드 중 27%가 helper 함수 의존으로 단독 실행 불가 | AST 기반 미정의 변수 검증 후 import 누락은 보정, 진짜 깨진 코드는 제외 |
| v6 학습 코드 작성 중 통제 변수 오염 (MAX_LENGTH, epoch까지 같이 변경됨) | fc(파일 비교)로 원본 train.py와 대조해 발견, 원래 값으로 복원 후 재학습 |
| v6(rank 32), v7(데이터 정제) 모두 응답 시간이 v5 대비 4~6배 급증 | 원인 미확정. EOS 토큰 학습 불안정성으로 추정되나 직접 검증은 못함(향후 과제) |
| SFT 학습 데이터(1,381개) 중 일부(2.8%)도 helper 함수 의존으로 깨짐 | DPO 데이터 검증과 동일한 AST 기법 적용해 1,354개로 정제(v7), 다만 v7 자체는 기각 |
| Level 1~2 재수집 시 SQL 문제 46개가 다시 섞여 들어옴 | 기존 필터 로직(is_sql_problem)이 Level 무관하게 동일 적용되어 자동 해결됨을 확인 |
| GitHub API 호출 중 ConnectTimeoutError 발생 | collect_github.py의 중간 저장 기능으로 재실행 시 이어서 진행, 일시적 네트워크 문제로 판단 |
| 신규 수집 GitHub 코드의 14.2%가 helper 함수 의존으로 깨짐(Level3 포함) | v7에서 만든 검증 스크립트를 그대로 재사용해 github_solutions_v2.json으로 정제 |
| v8에 repetition_penalty=1.3 적용 시 응답 속도는 v5 수준으로 회복했으나 코드 자체가 와해됨 | 추론 설정 문제가 아니라는 결론으로 이어짐, penalty 적용 포기 |
| problems_parsed.json의 examples 필드가 321개 전부 빈 배열로 수집됨 | fetch_problems.py의 표 파싱 셀렉터 문제로 추정, 실행 기반 검증 시도 중 발견, 미해결 |
| 10문제 평가에서 v5/v8 차이가 통계적으로 유의미한지 불확실 | 30문제로 평가셋 확장 후 재평가, 차이가 10%p에서 4%p로 축소됨을 확인 |
| train_selected.jsonl, val.jsonl이 v8 작업 중 덮어써져 v5 원본 데이터 소실 | git 커밋(fd2e98f)에서 원본을 git show로 복원, 별도 파일명으로 보존 |
| ifd_select.py 재실행 시 입력 데이터가 4,220개에서 4,011개로 의도치 않게 변경됨 | evol_dataset.py 등 중간 단계의 반복 패턴 정리 작업 영향으로 추정, 결과적으로 더 정제된 데이터가 됨을 확인 후 그대로 진행 |
| 학습 데이터(v8, 1,685개)의 30%가 같은 문제의 중복 GitHub 풀이였음 | 제목 기준 중복 제거 스크립트(clean_train_selected.py) 작성, 1,278개로 정제(v9) |
| 같은 모델의 30문제 평가 결과가 실행마다 약 10%p씩 달라짐 | temperature 샘플링과 채점 엄격도 차이로 추정, 절대값보다 모델 간 상대적 순위로 결론 도출 |

---

## 향후 가능한 방향 (이 프로젝트 범위 외)

- ai-coding-test-assistant에 v5 모델 연결 (FastAPI 로컬 서빙) — v5, v8, v9가 30문제 기준 통계적으로 동급이라 가장 먼저 만들어진 v5를 최종 모델로 확정
- v6, v7도 30문제로 재평가해 "v5보다 못하다"는 결론이 표본 노이즈였는지 v8/v9와 동일하게 검증
- 학습 시간 차이(v5 34시간 vs v6~v9 5~9시간)의 근본 원인 직접 추적 (시스템 리소스 모니터링 도구로 학습 중 RAM/디스크 상태 기록)
- 더 큰 모델(13B+) 또는 클라우드 GPU로 동일 실험 재현해 모델 크기 영향 검증 — 현재 조건(7B, 1,300~1,700개 데이터)에서는 데이터 조정만으로 40~55% 구간을 벗어나지 못함을 확인했으므로, 이 구간을 넘기려면 더 근본적인 변화가 필요
- 학습 데이터 규모를 320개 문제보다 더 크게(10배 이상) 확장 (다른 출처의 코딩테스트 문제 추가 수집)
- 모델 가중치 허깅페이스 Hub 업로드
- 코드 실행(테스트케이스) 기반 데이터 검증 파이프라인 구축. problems_parsed.json의 examples 필드가 처음부터 수집되지 않았음을 발견(전체 321개 모두 빈 배열). fetch_problems.py의 표 파싱 셀렉터(table tbody tr)가 실제 페이지 구조와 맞지 않는 것으로 추정되며, 이 부분을 먼저 고쳐야 실행 기반 검증이 가능하다
- 평가셋을 30문제보다 더 확장(50~100문제)해 통계적 신뢰도를 추가로 높이고, 자동 채점 파이프라인 구축

---

## 참고 논문

- Data-efficient LLM Fine-tuning for Code Generation — arXiv:2504.12687 — https://github.com/Kyle-Lyu/data-efficient-finetuning
- Finetune-RAG: Fine-Tuning Language Models to Resist Hallucination in RAG — arXiv:2505.10792 — https://github.com/Pints-AI/Finetune-Bench-RAG

---

## 프로젝트 운영 원칙

- 각 Phase 완료 후 정량 측정 결과 기록
- 실패 실험과 원인 분석도 그대로 문서화
- "초기 가설 -> 한계 -> 대안 -> 결과" 구조 유지
- 비교 실험은 한 번에 하나의 변수만 변경
- 하드웨어 한계 명확히 인식, 비용/효과 비율 떨어지면 전환
- 가설이 기각되더라도 솔직하게 기록하고 원인을 분석한다
- 같은 결과가 다른 조건에서도 반복되면, 조건 탓이 아니라 더 근본적인 원인을 재검토한다
- 한 번 만든 검증/평가 로직은 다음 단계에서도 재사용해 점검 도구를 축적한다
- 평가 결과의 절대값보다 여러 모델 간의 상대적 순위·격차를 신뢰한다 (실행마다 변동이 있을 수 있음)
- 평가/학습 스크립트는 각각 evaluation_scripts/, train_scripts/ 폴더로 모아 루트를 깔끔하게 유지한다